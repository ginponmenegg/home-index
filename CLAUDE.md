# HOME INDEX

住宅購入の診断サービス（https://homeindex.jp）。運営は 長沢 銀河（宅地建物
取引士）の個人事業。無料の購入診断（戸建・マンション・土地）と、有料のPRO。
**物件は売らない。**仲介も紹介もしないことが、このサービスの一番の武器。

---

## 仕事の振り分け（AIチーム）

**目的は、作業の効率化と容量の節約。** 調べもの・数え上げ・案出しで
本体の文脈を埋めないこと。

### Sonnet に出す（`.claude/agents/` のサブエージェント）

| 役割 | エージェント | 何をさせるか |
|---|---|---|
| 検索まわり | `seo-watch` | サイトの公開面の点検、検索での立ち位置、競合記事の分析 |
| SNS | `sns-watch` | 投稿の在庫、読まれている話題、切り口の分析、事実確認 |
| 市場・競合 | `market-watch` | 公的統計の収集、競合サービスの機能と料金、法改正の動き |
| アイデア | `idea-forge` | 記事のネタ、機能の案、投稿の切り口、文言の候補を**数で** |
| 集計・整理 | `data-crunch` | 取得済みデータの集計、欠損率、リポジトリの洗い出し |

**共通の約束**（各エージェントの定義にも書いてある）

- リポジトリのファイルを書き換えない（`data-crunch` はスクラッチパッドのみ）
- **`/diagnose` `/land_diagnose` などを叩かない**（診断件数が汚れる）
- **`/metrics` を開かない**（URLに鍵が入る）
- **不動産情報ライブラリのAPIを直接叩かない**（呼び出し回数を使う）
- **数字には必ず出典と、どう数えたかを添える**

### Opus（本体のセッション）でやる

サブエージェントに投げないこと。ここは**判断する側**。

- 最終判断、採否の決定
- 戦略、サービスの方向づけ
- サービス改善案の設計（サブの案を材料にして、こちらで組む）
- 最終評価、公開前のレビュー
- コードを書く・直す、テストを書く、コミット、プッシュ
- 法令・条文の確認（e-Gov で原文を引く）と、それを画面の文言にすること

**サブの報告を鵜呑みにしない。** 数字は出典を見て、重要なものは自分で
引き直す。サブが「一次情報が無い」と言ったら、それ自体が結論になることも
ある（地盤改良の費用がその例）。

---

## このサービスの決まりごと

- **根拠の無い数字を画面に出さない。** 相場の目安や費用のレンジは、
  一次情報が見つからなければ出さない。「いまは誰にも分からない」と書く。
- **分からない項目を埋めない。** 減点せず、情報充足度を下げて明示する。
  調べていない人を「悪い物件」として採点しない。
- **前提を隠さない。** 既定値で計算したら、そう書く。
- **運営者本人の体験を勝手に作らない。** 宅建士の肩書きで、実際には
  していない失敗談や経験を書かない。

## 開発の作法

- Windows。ファイルは CRLF と LF が混在しているので、**パッチを当てる前に
  必ず判別する**（`nl = "\r\n" if "\r\n" in s else "\n"`）。
- **`pytest ... | tail` は終了コードを隠す。** 必ず `RC=$?` を取る。
- **`sed` で複数行のHTMLを切らない。** 行単位なので静かに誤爆する。
  Python の文字列位置で切ること。
- **Jinja の辞書に `items` `keys` `values` をキー名として使わない。**
  メソッドが先に返って落ちる。
- `python app.py` は自動再読み込みしない。**変更したらサーバーを入れ替える。**
- 起動は `python app.py`（http://127.0.0.1:5000）。
