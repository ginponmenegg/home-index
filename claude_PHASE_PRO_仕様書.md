# HOME INDEX PRO 仕様書

Version 1.0 / 2026-08-19
対象リポジトリ：`ginponmenegg/home-index`

> **位置づけ**：機能・プラン・料金・技術構成は確定。§8 に残る未決事項は
> 実装と並行して決められるもののみ。本書をもとに実装指示書へ落とす。

---

## 1. 確定事項

- **課金対象**：購入検討者（一般消費者）＝ B2C
- **無料枠は撤廃**。**現在の診断は完全無料・無制限**とする（旧X案）
- **PRO は月額サブスクリプション**。内容は
  ①より詳細な物件評価 ②詳細な資金計画 ③リスク精査 ④比較 ⑤その他相談
- **PRO の価値は価格（円）の提示に置かない**
- **PRO初期リリースのスコープ**：§4 の A〜E **すべて**
- **月額料金**：**1,980円**（税込／税別は §8-1 で確定）
- **技術構成**：DB = Neon(PostgreSQL) ／ ログイン = マジックリンク ／
  決済 = Stripe ／ PDF = WeasyPrint

### 設計原則

> **点数・評価は売ってよい。円（価額）は売らない。**

不動産の鑑定評価に関する法律が規制するのは「経済価値を判定し、その結果を**価額に表示する**こと」。
100点スコア・グレード・リスク評価は価額ではないため、ここに線を引く。

**PRO では推定価格レンジの精度を上げない。** 現在の価格分析（`analyze_price`）の出力は
FREE と PRO で完全に同一とする。PRO で追加入力された情報（リフォーム内容・建物状態など）は
**物件スコアにのみ反映し、価格推定には渡さない**。

> ⚠️ この整理の妥当性、および現在の無料診断が価額を表示している点については、
> **有資格者への確認が未了**（§8-5）。確認結果によっては表示内容の見直しが必要になる。

### この決定による簡素化

無料枠を撤廃したことで、**利用回数のカウント機構が丸ごと不要**になった。
ユーザーごとの診断回数を数える必要も、その永続化も、上限到達時の分岐も要らない。
残るのは「ログインしているか」「サブスクが有効か」の2つの判定だけ。

現行のIP単位の回数制限（`_rate_ok`・40回/日）は**サーバ負荷対策として維持**する。
課金境界ではないため法的な位置づけが異なる。

---

## 2. PRO の中核コンセプト：情報充足度を上げる

現在の診断結果は「情報充足度 ○%」を表示している（`Diagnosis.data_sufficiency`）。
未確認の項目は評価に反映せず、充足度を下げる設計になっている（[src/scoring.py](src/scoring.py)）。

**PRO はこの充足度を上げるサービス**と位置づける。

```
FREE : 公開データ＋最小限の入力  → 情報充足度 60%前後の診断
PRO  : 詳細入力＋精査項目の追加  → 情報充足度 90%前後の診断
```

同じ物件でも FREE と PRO でスコアが変わるのは仕様である（情報が増えるため）。
これは既存コードの思想と完全に一致しており、利用者にも説明しやすい。

---

## 3. プラン設計

| | FREE | PRO |
|---|---|---|
| 料金 | 0円 | 月額（§6） |
| 会員登録 | 不要 | 必須 |
| 基本診断（100点スコア・グレード） | **無制限** | 無制限 |
| 推定価格レンジ・類似事例・㎡単価 | ○ | **○（FREEと同一）** |
| ハザード・用途地域・人口動向 | ○ | ○ |
| 住宅ローン | 月々返済額・返済負担率 | **詳細な資金計画**（§4-B） |
| 物件の詳細評価 | 築年・面積・駅距離のみ | **○**（§4-A） |
| リスク精査 | 基本ハザードのみ | **○**（§4-C） |
| 複数物件の比較 | × | **○**（§4-D） |
| 診断履歴の保存・再診断 | × | ○ |
| PDFレポート出力 | 画像保存のみ | **○** |
| 相談 | × | **○**（§4-E） |

---

## 4. PRO機能の内訳

### A. より詳細な物件評価
現在は未確認扱いのまま充足度を下げている項目を、入力を受けて評価に反映する。

- **建物内部状態** — 雨漏り・シロアリ・傾き・給排水・基礎のひび
  （現状は「建物内部の状態は未確認」と固定表示・[src/scoring.py:99](src/scoring.py:99)）
- **リフォーム内容の箇所別評価** — 水回り／外壁・屋根／内装／設備を個別に受ける
  （現状は有無のみで一律+8%）
- **設備の更新時期** — 給湯器・キッチン・浴室・電気設備
- **構造・工法・断熱性能**
- **マンションの管理の健全性** — 管理費・修繕積立金・積立金残高・大規模修繕履歴・管理形態
  ※**マンションMVP本体の完成が前提**

> ⚠️ **重要**：リフォーム内容は現在 `analyze_price` の中で推定価格に +8% しています
> （[src/price_analysis.py:156](src/price_analysis.py:156)）。PRO の箇所別入力は
> **この価格補正には接続せず、物件スコアのみに反映**します（§1の原則）。
> 実装時にここを取り違えないこと。

### B. 詳細な資金計画 ★最優先
[src/loan.py:5](src/loan.py:5) が「詳細な資金計画・諸費用・金利シナリオは有料版」と明言済み。

- 諸費用の内訳（仲介手数料・印紙税・登録免許税・不動産取得税・司法書士報酬・火災保険・引越し費用）
- 金利シナリオ（変動／固定、金利上昇時の返済額変化）
- 繰上返済シミュレーション
- 総支払額・住宅ローン控除の反映
- 年収からの適正借入額の逆算

**最優先にする理由**：
1. **物件の価額ではなく買い手の家計**を扱うため、§1の法的整理の結論に左右されない
2. 計算のみで完結し外部APIに依存しない＝原価ゼロ・実装が確実
3. 「いくらの物件か」より「自分に買えるか」のほうが、有料で知りたい情報になりやすい

> ⚠️ 個別の資金相談に踏み込むと金融商品仲介や有償の投資助言に接触し得ます。
> **一般的な試算の提示**にとどめ、個別の借入可否は断定しない設計とします（§8-6）。

### C. リスク精査
FREE の基本ハザード（洪水・土砂・津波・高潮・用途地域）を超える精査。

- 液状化（[README.md:6](README.md:6) で未実装と明記）
- 旧耐震／新耐震の判定と、1981〜1983年築の建築確認日の要確認フラグ
- 再建築可否に関わる確認項目（接道・市街化調整区域・越境・境界未確定）
- 対象物件に応じた「契約前に確認すべきこと」リストの生成

すべてスコアと要確認リストに反映し、**価格推定には反映しない**。

### D. 複数物件の比較
保存済みの診断を横並びにし、カテゴリ別スコア・リスク・資金計画を比較する。
**サブスクの継続動機として最も効く**（1件だけでは価値が出ないため）。

### E. その他相談
診断結果を添えて、宅建士など有資格者に相談できる導線。

> ⚠️ **要整理**：宅建業者が運営して媒介につなげる場合、広告規制・媒介契約の説明義務など
> 宅建業法上の論点が生じます。月額に含めるか都度課金かで特定商取引法上の表記も変わります（§8-4）。

---

## 5. システム構成

### 方針
既存の診断エンジン（`src/` 以下）には**一切手を入れない**。会員・課金・保存・PRO機能は
その外側にレイヤーとして足す。マンションMVPと同じく「既存を壊さない」を厳守する。

```
[ブラウザ]
    │
[Flask app.py]
    ├── 既存：/ , /diagnose , /mansion      ← 変更なし・誰でも無制限
    ├── 新規：/signup , /login , /logout , /mypage
    ├── 新規：/billing（Stripe Checkout）, /webhook（Stripe通知）
    └── 新規：/pro/diagnose , /history , /compare , /report.pdf
    │
[DB] users / diagnoses / subscriptions
    │
[Stripe] サブスクリプション課金
```

### アクセス判定（無料枠撤廃により大幅に簡素化）
```
基本診断（/diagnose・/mansion）
    → 誰でも実行可。ログイン不要。カウントなし
    → IP単位の負荷制限のみ維持

PRO機能（/pro/* , /history , /compare , /report.pdf）
    → ログイン中か？  No → ログイン/登録へ
    → サブスク有効か？ No → PRO案内へ
    → Yes → 実行
```

**回数カウントのテーブルもロジックも不要。** `subscriptions` の有効期限判定だけで足りる。

### 技術構成（確定）

| 領域 | 採用 | 理由 |
|---|---|---|
| DB | **Neon**（PostgreSQL） | 無料枠が期限切れにならない。SQLiteはRenderのファイルが揮発するため不可 |
| ログイン | **マジックリンク**（パスワードレス） | パスワードを保管しない＝漏洩リスクを負わない |
| 決済 | **Stripe** Checkout ＋ Customer Portal | カード情報が自社サーバに届かない。解約画面もStripe側 |
| PDF | **WeasyPrint** | HTMLから印刷品質のPDFを生成 |
| ホスティング | **Render 有料プラン** | 無料プランはスリープするため、課金ユーザーには使えない |

### ⚠️ メール送信サービスが1つ必要（§8-2で選定）

マジックリンクは**メールが届かなければログインが一切できません**。可用性が
そのままサービスの可用性になるため、送信基盤は慎重に選びます。

- 候補：Resend / SendGrid / Amazon SES
- **キャリアメール（docomo.ne.jp / ezweb.ne.jp 等）は迷惑メール判定が厳しく、
  届かない事故が起きやすい**。登録画面で「PCメール推奨」の注意書きを出し、
  届かない場合の問い合わせ導線を必ず用意すること
- 独自ドメインからの送信＋SPF/DKIM設定を前提とする

### データモデル

```
users
  id, email(unique), created_at, last_login_at

login_tokens          -- マジックリンク
  token_hash, email, expires_at(15分), used_at
  ※トークンは平文で保存せずハッシュ化する

subscriptions
  user_id, stripe_customer_id, stripe_subscription_id,
  status(active/past_due/canceled), current_period_end

diagnoses             -- PRO の履歴保存
  id, user_id, property_type(kodate/mansion),
  input_json, result_json, created_at
```

**診断結果は JSON でまるごと保存する。** 診断エンジンは今後も変化するため、
列に分解すると改修のたびにスキーマ変更が必要になる。`dataclasses.asdict()` で
シリアライズし、表示時に読み戻す。

> ⚠️ 保存する住所は「利用者が購入を検討している物件」であり、機微な情報です。
> 保存期間・削除方法をプライバシーポリシーに明記し、退会時の削除処理を用意すること。

### 主要フロー

**マジックリンクによるログイン**
```
メールアドレス入力
  → トークン生成（有効15分・使い捨て）、ハッシュを保存
  → 本文にリンクを載せて送信
  → リンク押下 → トークン検証 → 使用済みにする
  → セッション発行（90日保持。毎回メールを開かせない）
```

**Stripe によるサブスクリプション**
```
/billing → Stripe Checkout へ遷移（カード情報は自社を通らない）
  → 決済完了 → Stripe から /webhook へ通知
  → subscriptions を active で作成/更新
毎月の更新・解約・カード期限切れも同じ webhook で状態を同期する
解約・カード変更は Stripe Customer Portal に委譲（自前で作らない）
```

> ⚠️ webhook は**署名検証を必須**とする。検証しないと、外部から偽の
> 「課金成功」通知を送られてPRO機能を無料で開放されます。

---

## 6. 料金案

**注意**：以下は原価と想定継続期間からの試算。**競合サービスの実勢価格は未調査**であり、
最終決定前に市場価格の確認を推奨します（推測で数字は作りません）。

### 原価
- 外部API：不動産情報ライブラリ・e-Stat・国土地理院はいずれも**無料**
- サーバ：Render 有料プラン（スリープ回避）で月額数千円
- 決済手数料：Stripe で 3.6% 程度
- **診断1件あたりの限界費用はほぼゼロ**

### 3案

| | 月額 | 想定LTV(3〜6ヶ月) | 性格 |
|---|---|---|---|
| **A案** | 980円 | 2,940〜5,880円 | 障壁は最も低いが、件数を集めないと回収できない |
| **B案** | 1,980円 | 5,940〜11,880円 | 資金計画＋リスク精査＋比較に見合う中庸案 |
| **C案** | 3,980円 | 11,940〜23,880円 | 相談（§4-E）を月額に含めるなら成立し得る |

**→ 1,980円で確定**（B案）。相談（§4-E）も月額に含める前提とする。

無料枠を撤廃したことで、**PRO は基本診断の延長ではなく独立した価値として売る**必要が出ました。
「回数制限の解除」という分かりやすい動機が無くなったぶん、§4-B 資金計画の完成度が
そのまま転換率を左右します。

初月無料・年額プランは、購入検討期間が短い本サービスとは相性が悪いため不要と考えます。

---

## 7. 実装順序

法務対応（§9）と並行して進める。

1. **DB導入＋会員登録・ログイン**（診断機能には触らない）
2. **Stripe サブスクリプション**（課金・解約・Webhook）
3. **詳細な資金計画（§4-B）** ← PROの主力・法的整理に左右されない
4. **診断履歴の保存＋複数物件の比較（§4-D）**
5. **PDFレポート**
6. **より詳細な物件評価（§4-A）** ※価格補正に接続しないこと
7. **リスク精査（§4-C）** ※マンション管理はマンションMVP完了後
8. **相談の導線（§4-E）**

各段階で既存の無料診断フロー（`/` → `/diagnose`）が壊れていないことを確認する。

---

## 8. 未決事項

実装と並行して決められるもののみが残っている。

1. **1,980円は税込か税別か** — 総額表示義務があるため、表示は税込表記にする必要がある
2. **メール送信サービスの選定** — Resend / SendGrid / Amazon SES
3. **マンション診断の位置づけ** — FREEに含めるか、PRO限定にするか
4. **相談（§4-E）の運用** — 月額に含める前提。受付方法・回数上限・宅建業法上の整理
5. **§1の法的整理の裏取り** — 「点数は売る、円は売らない」で足りるか。
   現在の無料診断が価額を表示している点を含め、有資格者に確認
6. **§4-Bの踏み込み度** — 一般的試算にとどめる範囲の確定
7. **解約後の扱い** — 保存済み診断の閲覧可否、および退会時のデータ削除
8. **送信元ドメイン** — マジックリンクの送信に使う独自ドメインの用意

## 9. 課金開始前に必須の対応（法務・事務）

これらが揃うまで課金は開始できません。

1. **特定商取引法に基づく表記** — 現在「〔運営者名〕」のまま（[app.py:111](app.py:111)）。
   事業者名・住所・電話番号・返金/解約条件の明記が必要
2. **利用規約の改定** — 課金・自動更新・解約・返金の条項が現行規約にありません
3. **プライバシーポリシーの改定** — 会員情報・決済情報の取扱いを追記
4. **インボイス／適格請求書** — 課税事業者かどうかで対応が変わります
5. **§8-5 の法的整理**
